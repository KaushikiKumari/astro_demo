import 'package:flutter/material.dart';

class AppColors {
  static const backgroundColor = Colors.white;
  static const appbarBackgroundColor = Color.fromARGB(255, 3, 82, 100);
  static const whiteColor = Colors.white;
  static const borderColor = Colors.teal;
  static const greyColor = Colors.grey;
  static const mainColor = Color.fromARGB(255, 3, 82, 100);
}
